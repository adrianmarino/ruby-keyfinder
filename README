 Requisites
--------------------------------------------------------------------------------
 Steps:
  1. Install wine emulator:
     * Debian: $ sudo apt-get install wine
     * Arch: $ pacman -S wine
  2. Fix youtube_dl gem runing next command:
     $ bash fix_youtube_dl_gem
  3. Run: bash download_keyfinder
 To fast requisites installation run (Only for Debian): bash install_requisites 


 Test
--------------------------------------------------------------------------------
 Run test:
 $ rake test
